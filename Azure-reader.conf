<source>
  @type tail
  format /^(?<date>[^a-z[A-Z]]{19})\s*(?<host>[^ ]*)\s*(?<message>.*)$/
  path /opt/azure_trapper/trap_azu.log
  pos_file test.pos
  tag hatohol.messages
</source>

<match hatohol.**>
  @type stdout
</match>
